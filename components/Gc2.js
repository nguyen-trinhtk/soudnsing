import * as React from "react";
import { StyleSheet, View } from "react-native";

const Gc2 = () => {
  return <View style={styles.gc2} />;
};

const styles = StyleSheet.create({
  gc2: {
    width: 75,
    height: 75,
    opacity: 0.01,
  },
});

export default Gc2;
